import Foundation

enum IKO {
    static var set = "iko"
    static var count = 274
    
    static let cards = [
        AdaptiveShimmerer,
        Farfinder,
//        MysteriousEgg,
        BladeBanish,
        CheckpointOfficer,
//        CoordinatedCharge,
//        Cubwarden,
        DaysquadMarshal,
        DivineArrow,
//        DrannithHealer,
//        DrannithMagistrate,
//        FightAsOne,
//        FlourishingFox,
        GarrisonCat,
//        HelicaGlider,
//        HuntmasterLiger,
//        ImposingVantasaur,
//        KeensightMentor,
//        LavabrinkVenturer,
//        LightOfHope,
//        LuminousBroodmoth,
//        MajesticAuricorn,
        ManedServal,
//        MythosOfSnapdax,
        Pacifism,
        PatagiaTiger,
        PerimeterSergeant,
//        SanctuaryLockdown,
        SavaiSabertooth,
//        SnareTactician,
//        SolidFooting,
//        SplendorMare,
//        SpontaneousFlight,
//        StormwildCapridor,
//        SwallowWhole,
//        ValiantRescuer,
//        Vulpikeet,
//        WillOfTheAllHunter,
        AegisTurtle,
//        Anticipate,
//        Archipelagore,
//        AvianOddity,
//        BoonOfTheWishGiver,
        CaptureSphere,
//        Convolute,
        Crystacean,
//        DreamtailHeron,
//        EscapeProtocol,
        EssenceScatter,
        FacetReader,
        FrostLynx,
//        FrostveilAmbush,
        Glimmerbell,
        GustOfWind,
//        HamperingSnare,
//        KeepSafe,
//        MysticSubdual,
//        MythosOfIlluna,
//        Neutralize,
        OfOneMind,
//        OminousSeas,
        PhaseDolphin,
//        PollywogSymbiote,
//        PouncingShoreshark,
//        ReconnaissanceMission,
//        SeaDasherOctopus,
//        SharkTyphoon,
//        StartlingDevelopment,
        ThievingOtter,
        VoraciousGreatshark,
//        WingfoldPteron,
//        WingspanMentor,
        BastionOfRemembrance,
        BlitzLeech,
//        BloodCurdle,
//        BootNipper,
//        BushmeatPoacher,
//        CallOfTheDeathDweller,
//        CavernWhisperer,
//        ChitteringWhisperer,
//        CorpseChurn,
        DarkBargain,
        DeadWeight,
//        DirgeBat,
//        DurableCoilbug,
//        DuskfangMentor,
//        EasyPrey,
//        ExtinctionEvent,
        GloomPangolin,
//        Grimdancer,
//        HeartlessAct,
//        HuntedNightmare,
//        InsatiableHemophage,
//        LurkingDeadeye,
//        MemoryLeak,
//        MutualDestruction,
//        MythosOfNethroi,
        NightsquadCommando,
        SerratedScorpion,
//        SuffocatedFumes,
//        UnbreakableBond,
//        UnexpectedFangs,
        UnlikelyAid,
//        VoidBeckoner,
        WhisperSquad,
//        ZagothMamba,
        BlazingVolley,
        BlisterspitGremlin,
//        BlitzOfTheThunderRaptor,
//        CatharticReunion,
        ClashOfTitans,
//        Cloudpiercer,
//        DrannithStinger,
//        EverquillPhoenix,
//        FerociousTigorilla,
//        FireProphecy,
//        FlameSpill,
//        FootfallCrater,
        ForbiddenFriendship,
        FrenziedRaptor,
//        FrillscareMentor,
//        GoForBlood,
//        HeightenedReflexes,
//        LavaSerpent,
//        LukkaCoppercoatOutcast,
//        MomentumRumbler,
//        MythosOfVadrok,
//        Porcuparrot,
//        PricklyMarmoset,
        Pyroceratops,
//        RakingClaws,
//        ReptilianReflection,
//        RootingMoloch,
        RumblingRockslide,
//        SanctuarySmasher,
//        ShreddedSails,
        SpelleaterWolverine,
//        TentativeConnection,
//        UnpredictableCyclone,
//        WeaponizeTheMonsters,
//        YidaroWanderingMonster,
//        AdventurousImpulse,
        AlmightyBrushwagg,
//        AuspiciousStarrix,
//        BarrierBreach,
//        BristlingBoar,
//        ChargeOfTheForeverBeast,
        Colossification,
//        EssenceSymbiote,
        ExcavationMole,
        ExuberantWolfbear,
        Fertilid,
//        FlycatcherGiraffid,
//        FullyGrown,
//        Gemrazer,
//        GlowstoneRecluse,
//        GreaterSandwurm,
        HoneyMammoth,
//        HornbashMentor,
//        HumbleNaturalist,
//        Ivy Elemental,
        KoglaTheTitanApe,
//        Lead the Stampede,
//        MigrationPath,
//        MigratoryGreathorn,
//        MonstrousStep,
        MosscoatGoriak,
//        MythosOfBrokkos,
        Plummet,
//        RamThrough,
//        SuddenSpinnerets,
//        SurvivorsBond,
//        ThwartTheEnemy,
//        TitanothRex,
//        VivienMonstersAdvocate,
//        Wilt,
//        BackForMore,
//        BoneyardLurker,
//        BrokkosApexOfForever,
//        ChanneledForce,
//        ChevillBaneOfMonsters,
//        DeathsOasis,
        DireTactics,
//        EerieUltimatum,
//        EmergentUltimatum,
//        FrondlandFelidar,
//        GeneralKudroOfDrannith,
        GeneralsEnforcer,
//        GenesisUltimatum,
//        IllunaApexOfWishes,
        InspiredUltimatum,
//        KinnanBonderProdigy,
//        LabyrinthRaptor,
//        LoreDrakkis,
//        NarsetOfTheAncientWay,
//        Necropanther,
//        NethroiApexOfDeath,
//        OffspringsRevenge,
//        Parcelbeast,
//        PrimalEmpathy,
//        QuartzwoodCrasher,
//        RegalLeosaur,
//        RielleTheEverwise,
        RuinousUltimatum,
//        SavaiThundermane,
        SkullProphet,
        SkycatSovereign,
//        Slitherwisp,
//        SnapdaxApexOfTheHunt,
//        SongOfCreation,
        SpriteDragon,
//        TitansNest,
//        TrumpetingGnarr,
//        VadrokApexOfThunder,
        WhirlwindOfThought,
//        WinotaJoinerOfForces,
//        ZenithFlare,
        AlertHeedbonder,
//        CunningNightbonder,
//        FiendArtisan,
//        GyrudaDoomOfTheDepths,
//        JeganthaTheWellspring,
//        JubilantSkybonder,
//        KaheeraTheOrphanguard,
//        KerugaTheMacrosage,
//        LurrusOfTheDreamDen,
//        LutriTheSpellchaser,
//        OboshThePreypiercer,
//        ProudWildbonder,
//        SonoroudHowlbonder,
//        UmoriTheCollector,
//        YorionSkyNomad,
//        ZirdaTheDawnbreaker,
//        CrystallineGiant,
//        IndathaCrystal,
//        KetriaCrystal,
//        TheOzolith,
//        RaugrinCrystal,
//        SavaiCrystal,
        SleeperDart,
        SpringjawTrap,
//        ZagothCrystal,
        BloodfellCaves,
        BlossomingSands,
//        BondersEnclave,
        DismalBackwater,
        EvolvingWilds,
//        IndathaTriome,
        JungleHollow,
//        KetriaTriome,
//        RaugrinTriome,
        RuggedHighlands,
//        SavaiTriome,
        ScouredBarrens,
        SwiftwaterCliffs,
        ThornwoodFalls,
        TranquilCove,
        WindScarredCrag,
//        ZagothTriome,
    ]

    static func RandomCard() -> Card {
        return cards[Int.random(in: 0 ..< cards.count)]()
    }
    
    static func AdaptiveShimmerer() -> Card {
        let adaptiveShimmerer = Card(name: "Adaptive Shimmerer", rarity: .Common, set: set, number: 1)
        adaptiveShimmerer.setManaCost("5")
        adaptiveShimmerer.setType(.Creature, .Insect)
        adaptiveShimmerer.flash = true
        adaptiveShimmerer.addReplacementEffect(
            event: .ThisETB,
            effect: { adaptiveShimmerer.addCounters(.PlusOnePlusOne, 3) })
        adaptiveShimmerer.setFlavorText("\"You sure you want to see what emerges? You might not like it. It definitely won't like you.\"\n--Kinnan, bonder prodigy")
        adaptiveShimmerer.power = 0
        adaptiveShimmerer.toughness = 0
        return adaptiveShimmerer
    }
    static func Farfinder() -> Card {
        let farfinder = Card(name: "Farfinder", rarity: .Common, set: set, number: 2)
        farfinder.setManaCost("3")
        farfinder.setType(.Creature, .Fox)
        farfinder.vigilance = true
        farfinder.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { farfinder.getController().chooseCard(
                from: farfinder.getController().getLibrary(),
                restriction: { $0.isBasicLand() },
                action: { chosen, rest in
                    chosen?.reveal()
                    chosen?.putIntoHand()
                    farfinder.getController().shuffleLibrary()
                })
            },
            effectOptional: true)
        farfinder.setFlavorText("\"Take us home,\" she whispered, and the fox's eyes began to glow.")
        farfinder.power = 1
        farfinder.toughness = 1
        return farfinder
    }
    // 3 Mysterious Egg
    static func BladeBanish() -> Card {
        let bladeBanish = Card(name: "Blade Banish", rarity: .Common, set: set, number: 4)
        bladeBanish.setManaCost("3W")
        bladeBanish.setType(.Instant)
        bladeBanish.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.SingleObject(
                restriction: { $0.isType(.Creature) && $0.getPower() >= 4 },
                zones: [.Battlefield]),
            effect: { target in
                _ = target.exile()
        }))
        bladeBanish.setFlavorText("The Wanderer walks many paths. Her blade travels only one.")
        return bladeBanish
    }
    static func CheckpointOfficer() -> Card {
        let checkpointOfficer = Card(name: "Checkpoint Officer", rarity: .Common, set: set, number: 5)
        checkpointOfficer.setManaCost("1W")
        checkpointOfficer.setType(.Creature, .Human, .Soldier)
        checkpointOfficer.addActivatedAbility(
            string: "{1}{W}, {T}: Tap target creature.",
            cost: Cost.Mana("1W").Tap(),
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.TargetCreature(),
                effect: { $0.tap() }))
        checkpointOfficer.setFlavorText("After what he viewed as Lukkan's shocking betrayal, General Kudro enacted stringent security measures at every entry point into Drannith.")
        checkpointOfficer.power = 1
        checkpointOfficer.toughness = 2
        return checkpointOfficer
    }
    // 6 Coordinated Charge
    // 7 Cubwarden
    static func DaysquadMarshal() -> Card {
        let daysquadMarshal = Card(name: "Daysquad Marshal", rarity: .Common, set: set, number: 8)
        daysquadMarshal.setManaCost("3W")
        daysquadMarshal.setType(.Creature, .Human, .Soldier)
        daysquadMarshal.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { daysquadMarshal.getController().createToken(HumanSoldier()) })
        daysquadMarshal.setFlavorText("\"The night emboldens many monsters, but the mightiest will attack in broad daylight.\"")
        daysquadMarshal.power = 3
        daysquadMarshal.toughness = 3
        return daysquadMarshal
    }
    static func DivineArrow() -> Card {
        let divineArrow = Card(name: "Divine Arrow", rarity: .Common, set: set, number: 9)
        divineArrow.setManaCost("1W")
        divineArrow.setType(.Instant)
        divineArrow.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.SingleObject(
                restriction: { $0.isType(.Creature) && ($0.isAttacking || $0.blocking ) },
                zones: [.Battlefield]),
            effect: { target in divineArrow.damage(to: target, 4) }))
        divineArrow.setFlavorText("Outside Drannith's walls, where collateral damage is of no concern, the Coppercoats are free to use more potent methods.")
        return divineArrow
    }
    // 10 Drannith Healer
    // 11 Drannith Magistrate
    // 12 Fight as One
    // 13 Flourishing Fox
    static func GarrisonCat() -> Card {
        let garrisonCat = Card(name: "Garrison Cat", rarity: .Common, set: set, number: 14)
        garrisonCat.setManaCost("W")
        garrisonCat.setType(.Creature, .Cat)
        garrisonCat.addTriggeredAbility(
            trigger: .ThisDies,
            effect: { garrisonCat.getController().createToken(HumanSoldier()) })
        garrisonCat.setFlavorText("After much arguing, the soldiers decided to keep it, on the grounds that it killed enough mice to count as an honorary monster slayer.")
        garrisonCat.power = 1
        garrisonCat.toughness = 1
        return garrisonCat
    }
    // 15 Helica Glider
    // 16 Huntmaster Liger
    // 17 Imposing Vantasaur
    // 18 Keensight Mentor
    // 19 Lavabrink Venturer
    // 20 Light of Hope
    // 21 Luminous Broodmoth
    // 22 Majestic Auricorn
    static func ManedServal() -> Card {
        let manedServal = Card(name: "Maned Serval", rarity: .Common, set: set, number: 23)
        manedServal.setManaCost("1W")
        manedServal.setType(.Creature, .Cat)
        manedServal.vigilance = true
        manedServal.setFlavorText("\"Don't search the ruins of Orn during the day. Nethroi's many eyes will spot you instantly. Don't go at night either. You'll be dinner for the servals. Maybe just don't go.\"\n--Rasai, Indatha hunter")
        manedServal.power = 1
        manedServal.toughness = 4
        return manedServal
    }
    // 24 Mythos of Snapdax
    static func Pacifism() -> Card {
        let pacifism = Card(name: "Pacifism", rarity: .Common, set: set, number: 25)
        pacifism.setManaCost("1W")
        pacifism.setType(.Enchantment, .Aura)
        pacifism.addEnchantAbility(
            restriction: TargetingRestriction.TargetCreature(),
            effects: [
                ({ return $0.withKeyword(.CantAttack) }, .AbilityAddingOrRemoving),
                ({ return $0.withKeyword(.CantBlock) }, .AbilityAddingOrRemoving)
            ])
        pacifism.setFlavorText("\"If only they'd all act this docile. I could do something relaxing for a living. I've always wanted to bake biscuits.\"\n--Ethuk, Coppercoat mage.")
        return pacifism
    }
    static func PatagiaTiger() -> Card {
        let patagiaTiger = Card(name: "Patagia Tiger", rarity: .Common, set: set, number: 26)
        patagiaTiger.setManaCost("4W")
        patagiaTiger.setType(.Creature, .Cat)
        patagiaTiger.flying = true
        patagiaTiger.addTriggeredAbility(
            trigger: .ThisETB,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isType(.Human) && $0.getController() === patagiaTiger.getController() },
                    zones: [.Battlefield]),
                effect: { $0.pump(2, 2) }))
        patagiaTiger.setFlavorText("Lukka looked out from the parapet and saw not a monster to be put down, but a fierce kind of beauty.")
        patagiaTiger.power = 3
        patagiaTiger.toughness = 4
        return patagiaTiger
    }
    static func PerimeterSergeant() -> Card {
        let perimeterSergeant = Card(name: "Perimeter Sergeant", rarity: .Common, set: set, number: 27)
        perimeterSergeant.setManaCost("2W")
        perimeterSergeant.setType(.Creature, .Human, .Soldier)
        perimeterSergeant.addTriggeredAbility(
            trigger: .ThisAttacks,
            effect: {
                perimeterSergeant.getController().getPermanents().filter({ $0.isType(.Human) && $0 != perimeterSergeant }).forEach({ $0.pump(1, 0) })
        })
        perimeterSergeant.setFlavorText("\"Get in there, soldier! I want a pelt on my wall before lunch!\"")
        perimeterSergeant.power = 3
        perimeterSergeant.toughness = 2
        return perimeterSergeant
    }
    // 28 Sanctuary Lockdown
    static func SavaiSabertooth() -> Card {
        let savaiSabertooth = Card(name: "Savai Sabertooth", rarity: .Common, set: set, number: 29)
        savaiSabertooth.setManaCost("1W")
        savaiSabertooth.setType(.Creature, .Cat)
        savaiSabertooth.setFlavorText("TODO")
        savaiSabertooth.power = 3
        savaiSabertooth.toughness = 1
        return savaiSabertooth
    }
    // 30 Snare Tactician
    // 31 Solid Footing
    // 32 Splendor Mare
    // 33 Spontaneous Flight
    // 34 Stormwild Capridor
    // 35 Swallow Whole
    // 36 Valiant Rescuer
    // 37 Vulpikeet
    // 38 Will of the All-Hunter
    static func AegisTurtle() -> Card {
        let aegisTurtle = Card(name: "Aegis Turtle", rarity: .Common, set: set, number: 39)
        aegisTurtle.setManaCost("U")
        aegisTurtle.setType(.Creature, .Turtle)
        aegisTurtle.setFlavorText("todo")
        aegisTurtle.power = 0
        aegisTurtle.toughness = 5
        return aegisTurtle
    }
    // 40 Anticipate
    // 41 Archipelagore
    // 42 Avian Oddity
    // 43 Boon of the Wish-Giver
    static func CaptureSphere() -> Card {
        let captureSphere = Card(name: "Capture Sphere", rarity: .Common, set: set, number: 44)
        captureSphere.setManaCost("3U")
        captureSphere.setType(.Enchantment, .Aura)
        captureSphere.flash = true
        captureSphere.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { captureSphere.attachedTo?.tap() })
        captureSphere.addEnchantAbility(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { $0.withoutKeyword(.UntapsDuringUntapStep)},
            layer: .AbilityAddingOrRemoving)
        return captureSphere
    }
    // 45 Convolute
    static func Crystacean() -> Card {
        let crystacean = Card(name: "Crystacean", rarity: .Common, set: set, number: 46)
        crystacean.setManaCost("3U")
        crystacean.setType(.Creature, .Crab)
        crystacean.flash = true
        crystacean.setFlavorText("todo")
        crystacean.power = 1
        crystacean.toughness = 6
        return crystacean
        
    }
    // 47 Dreamtail Heron
    // 48 Escape Protocol
    static func EssenceScatter() -> Card {
        let essenceScatter = Card(name: "Essence Scatter", rarity: .Common, set: set, number: 49)
        essenceScatter.setManaCost("1U")
        essenceScatter.setType(.Instant)
        essenceScatter.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.TargetCreatureSpell(),
            effect: { $0.counter() }))
        essenceScatter.setFlavorText("todo")
        return essenceScatter
    }
    static func FacetReader() -> Card {
        let facetReader = Card(name: "Facet Reader", rarity: .Common, set: set, number: 50)
        facetReader.setManaCost("1U")
        facetReader.setType(.Creature, .Human, .Wizard)
        facetReader.addActivatedAbility(
            string: "{1}, {T}: Draw a card, then discard a card.",
            cost: Cost.Mana("1").Tap(),
            effect: {
                facetReader.getController().drawCard()
                facetReader.getController().discard()
        })
        facetReader.setFlavorText("todo")
        facetReader.power = 1
        facetReader.toughness = 2
        return facetReader
    }
    static func FrostLynx() -> Card {
        let frostLynx = Card(name: "Frost Lynx", rarity: .Common, set: set, number: 51)
        frostLynx.setManaCost("2U")
        frostLynx.setType(.Creature, .Elephant, .Cat)
        frostLynx.addTriggeredAbility(
            trigger: .ThisETB,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isType(.Creature) && $0.getController() !== frostLynx.getController() },
                    zones: [.Battlefield]),
                effect: { target in
                    target.tap()
                    target.untapsDuringNextUntapStep = false
            }))
        frostLynx.setFlavorText("todo")
        frostLynx.power = 2
        frostLynx.toughness = 2
        return frostLynx
    }
    // 52 Frostveil Ambush
    static func Glimmerbell() -> Card {
        let glimmerbell = Card(name: "Glimmerbell", rarity: .Common, set: set, number: 53)
        glimmerbell.setManaCost("1U")
        glimmerbell.setType(.Creature, .Elemental, .Jellyfish)
        glimmerbell.flying = true
        glimmerbell.addActivatedAbility(
            string: "{1}{U}: Untap ~.",
            cost: Cost.Mana("1U"),
            effect: { glimmerbell.untap() })
        glimmerbell.setFlavorText("todo")
        glimmerbell.power = 1
        glimmerbell.toughness = 3
        return glimmerbell
    }
    static func GustOfWind() -> Card {
        let gustOfWind = Card(name: "Gust of Wind", rarity: .Common, set: set, number: 54)
        gustOfWind.setManaCost("3U")
        gustOfWind.setType(.Sorcery)
        gustOfWind.addStaticAbility(
            requirement: AbilityRequirement.This(gustOfWind),
            effect: { object in
                if !object.getController().getCreatures().filter({ $0.flying }).isEmpty {
                    object.castingCost = object.getBaseCastingCost().reducedBy(2)
                }
                return object
            },
            layer: .CostReduction,
            allZones: true)
        gustOfWind.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.SingleObject(
                restriction: { !$0.isLand() && $0.isPermanent() && $0.getController() !== gustOfWind.getController() },
                zones: [.Battlefield]),
            effect: { target in
                target.bounce()
                gustOfWind.getController().drawCard()
        }))
        gustOfWind.setFlavorText("todo")
        return gustOfWind
    }
    // 55 Hampering Snare
    // 56 Keep Safe
    // 57 Mystic Subdual
    // 58 Mythos of Illuna
    // 59 Neutralize
    static func OfOneMind() -> Card {
        let ofOneMind = Card(name: "Of One Mind", rarity: .Common, set: set, number: 60)
        ofOneMind.setManaCost("2U")
        ofOneMind.setType(.Sorcery)
        ofOneMind.addStaticAbility(
            requirement: AbilityRequirement.This(ofOneMind),
            effect: { object in
                let controlsHuman = !object.getController().getCreatures().filter({ $0.isType(.Human) }).isEmpty
                let controlsNonhuman = !object.getController().getCreatures().filter({ !$0.isType(.Human) }).isEmpty
                if controlsHuman && controlsNonhuman {
                    object.castingCost = object.getBaseCastingCost().reducedBy(2)
                }
                return object
            },
            layer: .CostReduction,
            allZones: true)
        ofOneMind.addEffect({
            ofOneMind.getController().drawCard()
        })
        ofOneMind.setFlavorText("todo")
        return ofOneMind
    }
    // 61 Ominous Seas
    static func PhaseDolphin() -> Card {
        let phaseDolphin = Card(name: "Phase Dolphin", rarity: .Common, set: set, number: 62)
        phaseDolphin.setManaCost("2U")
        phaseDolphin.setType(.Creature, .Elemental, .Whale)
        phaseDolphin.addTriggeredAbility(
            trigger: .ThisAttacks,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0 != phaseDolphin && $0.isAttacking && $0.getController() === phaseDolphin.getController() },
                    zones: [.Battlefield]),
                effect: { $0.giveKeywordUntilEndOfTurn(.Unblockable) }))
        phaseDolphin.setFlavorText("todo")
        phaseDolphin.power = 1
        phaseDolphin.toughness = 4
        return phaseDolphin
    }
    // 63 Pollywog Symbiote
    // 64 Pouncing Shoreshark
    // 65 Reconnaissance Mission
    // 66 Sea-Dasher Octopus
    // 67 Shark Typhoon
    // 68 Startling Development
    static func ThievingOtter() -> Card {
        let thievingOtter = Card(name: "Thieving Otter", rarity: .Common, set: set, number: 69)
        thievingOtter.setManaCost("2U")
        thievingOtter.setType(.Creature, .Otter)
        thievingOtter.addTriggeredAbility(
            trigger: .ThisDealsDamageToOpponent,
            effect: { thievingOtter.getController().drawCard() })
        thievingOtter.setFlavorText("todo")
        thievingOtter.power = 2
        thievingOtter.toughness = 2
        return thievingOtter
    }
    static func VoraciousGreatshark() -> Card {
        let voraciousGreatshark = Card(name: "Voracious Greatshark", rarity: .Rare, set: set, number: 70)
        voraciousGreatshark.setManaCost("3UU")
        voraciousGreatshark.setType(.Creature, .Shark)
        voraciousGreatshark.flash = true
        voraciousGreatshark.addTriggeredAbility(
            trigger: .ThisETB,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isSpell() && ($0.isArtifact() || $0.isCreature()) },
                    zones: [.Stack]),
                effect: { $0.counter() }))
        voraciousGreatshark.setFlavorText("todo")
        voraciousGreatshark.power = 5
        voraciousGreatshark.toughness = 4
        return voraciousGreatshark
    }
    // 71 Winged Pteron
    // 72 Wingspan Mentor
    static func BastionOfRemembrance() -> Card {
        let bastionOfRemembrance = Card(name: "Bastion of Remembrance", rarity: .Uncommon, set: set, number: 73)
        bastionOfRemembrance.setManaCost("2B")
        bastionOfRemembrance.setType(.Enchantment)
        bastionOfRemembrance.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { bastionOfRemembrance.getController().createToken(HumanSoldier()) })
        bastionOfRemembrance.addTriggeredAbility(
            trigger: .CreatureYouControlDies,
            effect: {
                bastionOfRemembrance.eachOpponent({ $0.loseLife(1) })
                bastionOfRemembrance.getController().gainLife(1)
        })
        bastionOfRemembrance.setFlavorText("todo")
        return bastionOfRemembrance
        
    }
    static func BlitzLeech() -> Card {
        let blitzLeech = Card(name: "Blitz Leech", rarity: .Common, set: set, number: 74)
        blitzLeech.setManaCost("5B")
        blitzLeech.setType(.Creature, .Leech)
        blitzLeech.flash = true
        blitzLeech.addTriggeredAbility(
            trigger: .ThisETB,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.TargetCreatureAnOpponentControl(source: blitzLeech),
                effect: { target in
                    target.pump(-2, -2)
                    target.removeAllCounters()
            }))
        blitzLeech.setFlavorText("todo")
        blitzLeech.power = 5
        blitzLeech.toughness = 2
        return blitzLeech
        
    }
    // 75 Blood Curdle
    // 76 Boot Nipper
    // 77 Bushmeat Poacher
    // 78 Call of Death-Dweller
    // 79 Cavern Whisperer
    // 80 Chittering Harvester
    // 81 Corpse Churn
    static func DarkBargain() -> Card {
        let darkBargain = Card(name: "Dark Bargain", rarity: .Common, set: set, number: 82)
        darkBargain.setManaCost("3B")
        darkBargain.setType(.Instant)
        darkBargain.addEffect({
            darkBargain.getController().chooseCards(
                from: darkBargain.getController().getLibrary().suffix(3),
                restrictions: [{ _ in return true }, { _ in return true }],
                action: { chosen, rest in
                    chosen.forEach({ $0.putIntoHand() })
                    rest.forEach({ $0.putIntoGraveyard() })
            })
        })
        darkBargain.setFlavorText("todo")
        return darkBargain
    }
    static func DeadWeight() -> Card {
        let deadWeight = Card(name: "Dead Weight", rarity: .Common, set: set, number: 83)
        deadWeight.setManaCost("B")
        deadWeight.setType(.Enchantment, .Aura)
        deadWeight.addEnchantAbility(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { return $0.pumped(-2, -2) },
            layer: .PowerToughnessChanging)
        deadWeight.setFlavorText("todo")
        return deadWeight
    }
    // 84 Dirge Bat
    // 85 Durable Coilbug
    // 86 Duskfang Mentor
    // 87 Easy Prey
    // 88 Extinction Event
    static func GloomPangolin() -> Card {
        let gloomPangolin = Card(name: "Gloom Pangolin", rarity: .Common, set: set, number: 89)
        gloomPangolin.setManaCost("2B")
        gloomPangolin.setType(.Creature, .Nightmare, .Pangolin)
        gloomPangolin.setFlavorText("todo")
        gloomPangolin.power = 1
        gloomPangolin.toughness = 5
        return gloomPangolin
    }
    // 90 Grimdancer
    // 91 Heartless Act
    // 92 Hunted Nightmare
    // 93 Insatiable Hemophage
    // 94 Lurking Deadeye
    // 95 Memory Leak
    // 96 Mutual Destruction
    // 97 Mythos of Nethroi
    static func NightsquadCommando() -> Card {
        let nightsquadCommando = Card(name: "Nightsquad Commando", rarity: .Common, set: set, number: 98)
        nightsquadCommando.setManaCost("2B")
        nightsquadCommando.setType(.Creature, .Human, .Soldier)
        nightsquadCommando.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { nightsquadCommando.getController().createToken(HumanSoldier()) },
            restriction: { nightsquadCommando.getController().attackedWithCreatureThisTurn })
        nightsquadCommando.setFlavorText("todo")
        nightsquadCommando.power = 2
        nightsquadCommando.toughness = 3
        return nightsquadCommando
    }
    static func SerratedScorpion() -> Card {
        let serratedScorpion = Card(name: "Serrated Scorpion", rarity: .Common, set: set, number: 99)
        serratedScorpion.setManaCost("B")
        serratedScorpion.setType(.Creature, .Scorpion)
        serratedScorpion.addTriggeredAbility(
            trigger: .ThisDies,
            effect: {
                serratedScorpion.eachOpponent({ $0.loseLife(2) })
                serratedScorpion.getController().gainLife(2);
        })
        serratedScorpion.setFlavorText("todo")
        serratedScorpion.power = 1
        serratedScorpion.toughness = 2
        return serratedScorpion
    }
    // 100 Suffocating Fumes
    // 101 Unbreakable Bond
    // 102 Unexpected Fangs
    static func UnlikelyAid() -> Card {
        let unlikelyAid = Card(name: "Unlikely Aid", rarity: .Common, set: set, number: 103)
        unlikelyAid.setManaCost("1B")
        unlikelyAid.setType(.Instant)
        unlikelyAid.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { target in
                target.pump(2, 0)
                target.giveKeywordUntilEndOfTurn(.Indestructible)
        }))
        unlikelyAid.setFlavorText("todo")
        return unlikelyAid
    }
    // 104 Void Beckoner
    static func WhisperSquad() -> Card {
        let name = "Whisper Squad"
        let whisperSquad = Card(name: name, rarity: .Common, set: set, number: 105)
        whisperSquad.setManaCost("B")
        whisperSquad.setType(.Creature, .Human, .Soldier)
        whisperSquad.addActivatedAbility(
            string: "{1}{B}: Search your library for a card named " + name + ", put it onto the battlefield tapped, then shuffle your library.",
            cost: Cost.Mana("1B"),
            effect: {
                whisperSquad.getController().chooseCard(
                    from: whisperSquad.getController().getLibrary(),
                    restriction: { $0.getName() == name },
                    action: { chosen, rest in
                        chosen?.putOntoBattlefield(tapped: true)
                        whisperSquad.getController().shuffleLibrary()
                })
        })
        whisperSquad.setFlavorText("todo")
        whisperSquad.power = 1
        whisperSquad.toughness = 1
        return whisperSquad
    }
    // 106 Zagoth Mamba
    static func BlazingVolley() -> Card {
        let blazingVolley = Card(name: "Blazing Volley", rarity: .Common, set: set, number: 107)
        blazingVolley.setManaCost("R")
        blazingVolley.setType(.Sorcery)
        blazingVolley.addEffect({
            blazingVolley.getOpponent().getCreatures().forEach({ blazingVolley.damage(to: $0, 1) })
        })
        blazingVolley.setFlavorText("todo")
        return blazingVolley
    }
    static func BlisterspitGremlin() -> Card {
        let blisterspitGremlin = Card(name: "Blisterspit Gremlin", rarity: .Common, set: set, number: 108)
        blisterspitGremlin.setManaCost("R")
        blisterspitGremlin.setType(.Creature, .Gremlin)
        blisterspitGremlin.addActivatedAbility(
            string: "{1}, {T}: ~ deals 1 damage to each opponent.",
            cost: Cost.Mana("1").Tap(),
            effect: { blisterspitGremlin.damage(to: blisterspitGremlin.getOpponent(), 1) })
        blisterspitGremlin.addTriggeredAbility(
            trigger: .YouCastNoncreatureSpell,
            effect: { blisterspitGremlin.untap() })
        blisterspitGremlin.setFlavorText("todo")
        blisterspitGremlin.power = 1
        blisterspitGremlin.toughness = 1
        return blisterspitGremlin
    }
    // 109 Blitz of the Thunder-Raptor
    // 110 Cathartic Reunion
    static func ClashOfTitans() -> Card {
        let clashOfTitans = Card(name: "Clash of Titans", rarity: .Uncommon, set: set, number: 111)
        clashOfTitans.setManaCost("3RR")
        clashOfTitans.setType(.Instant)
        clashOfTitans.addEffect(TargetedEffect.MultiObject(
            restrictions: [TargetingRestriction.TargetCreature(), TargetingRestriction.TargetCreature()],
            effect: { targets in
                if let target1 = targets[0], let target2 = targets[1] {
                    target1.fight(target2)
                }
        }))
        clashOfTitans.setFlavorText("todo")
        return clashOfTitans
    }
    // 112 Cloudpiercer
    // 113 Drannith Stinger
    // 114 Everquill Phoenix
    // 115 Ferocious Tigorilla
    // 116 Fire Prophecy
    // 117 Flame Spill
    // 118 Footfall Crater
    static func ForbiddenFriendship() -> Card {
        let forbiddenFriendship = Card(name: "Forbidden Friendship", rarity: .Common, set: set, number: 119)
        forbiddenFriendship.setManaCost("1R")
        forbiddenFriendship.setType(.Sorcery)
        forbiddenFriendship.addEffect {
            forbiddenFriendship.getController().createToken(Dinosaur())
            forbiddenFriendship.getController().createToken(HumanSoldier())
        }
        forbiddenFriendship.setFlavorText("The two looked at each other, and neither fled. \"I see,\" the soldier murmured. \"We're both just fighting for survival out here, aren't we?\"")
        return forbiddenFriendship
    }
    static func FrenziedRaptor() -> Card {
        let frenziedRaptor = Card(name: "Frenzied Raptor", rarity: .Common, set: set, number: 120)
        frenziedRaptor.setManaCost("2R")
        frenziedRaptor.setType(.Creature, .Dinosaur)
        frenziedRaptor.setFlavorText("todo")
        frenziedRaptor.power = 4
        frenziedRaptor.toughness = 2
        return frenziedRaptor
    }
    // 121 Frillscare Mentor
    // 122 Go for Blood
    // 123 Heightened Reflexes
    // 124 Lava Serpent
    // 125 Lukka, Coppercoat Outcast
    // 126 Momentum Rumbler
    // 127 Mythos of Vadrok
    // 128 Porcuparrot
    // 129 Prickly Marmoset
    static func Pyroceratops() -> Card {
        let pyroceratops = Card(name: "Pyroceratops", rarity: .Common, set: set, number: 130)
        pyroceratops.setManaCost("3R")
        pyroceratops.setType(.Creature, .Elemental, .Dinosaur)
        pyroceratops.trample = true
        pyroceratops.addTriggeredAbility(
            trigger: .YouCastNoncreatureSpell,
            effect: { pyroceratops.addCounter(.PlusOnePlusOne) })
        pyroceratops.setFlavorText("todo")
        pyroceratops.power = 2
        pyroceratops.toughness = 3
        return pyroceratops
    }
    // 131 Raking Claws
    // 132 Reptilian Reflection
    // 133 Rooting Moloch
    static func RumblingRockslide() -> Card {
        let rumblingRockslide = Card(name: "Rumbling Rockslide", rarity: .Common, set: set, number: 134)
        rumblingRockslide.setManaCost("3R")
        rumblingRockslide.setType(.Sorcery)
        rumblingRockslide.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { target in
                let numLands = rumblingRockslide.getController().getLands().count
                rumblingRockslide.damage(to: target, numLands)
        }))
        rumblingRockslide.setFlavorText("todo")
        return rumblingRockslide
    }
    // 135 Sanctuary Smasher
    // 136 Shredded Sails
    static func SpelleaterWolverine() -> Card {
        let spelleaterWolverine = Card(name: "Spelleater Wolverine", rarity: .Common, set: set, number: 137)
        spelleaterWolverine.setManaCost("2R")
        spelleaterWolverine.setType(.Creature, .Wolverine)
        spelleaterWolverine.addStaticAbility(
            requirement: AbilityRequirement.This(spelleaterWolverine),
            effect: { object in
                if object.getController().getGraveyard().filter({ $0.isInstantOrSorcery() }).count >= 3 {
                    return object.withKeyword(.DoubleStrike)
                }
                return object
            },
            layer: .AbilityAddingOrRemoving)
        spelleaterWolverine.setFlavorText("todo")
        spelleaterWolverine.power = 3
        spelleaterWolverine.toughness = 2
        return spelleaterWolverine
    }
    // 138 Tentative Connection
    // 139 Unpredictable Cyclone
    // 140 Weaponize the Monsters
    // 141 Yidaro, Wandering Monster
    // 142 Adventurous Impulse
    static func AlmightyBrushwagg() -> Card {
        let almightyBrushwagg = Card(name: "Almighty Brushwagg", rarity: .Common, set: set, number: 143)
        almightyBrushwagg.setManaCost("G")
        almightyBrushwagg.setType(.Creature, .Brushwagg)
        almightyBrushwagg.trample = true
        almightyBrushwagg.addActivatedAbility(
            string: "{3}{G}: ~ gets +3/+3 until end of turn.",
            cost: Cost.Mana("3G"),
            effect: { almightyBrushwagg.pump(3, 3) })
        almightyBrushwagg.setFlavorText("todo")
        almightyBrushwagg.power = 1
        almightyBrushwagg.toughness = 1
        return almightyBrushwagg
    }
    // 144 Auspicious Starrix
    // 145 Barrier Breach
    // 146 Bristling Boar
    // 147 Charge of the Forever-Beast
    static func Colossification() -> Card {
        let colossification = Card(name: "Colossification", rarity: .Rare, set: set, number: 148)
        colossification.setManaCost("5GG")
        colossification.setType(.Enchantment, .Aura)
        colossification.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { colossification.attachedTo?.tap() })
        colossification.addEnchantAbility(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { $0.pumped(20, 20) },
            layer: .PowerToughnessChanging)
        colossification.setFlavorText("todo")
        return colossification
    }
    // 149 Essence Symbiote
    static func ExcavationMole() -> Card {
        let excavationMole = Card(name: "Excavation Mole", rarity: .Common, set: set, number: 150)
        excavationMole.setManaCost("2G")
        excavationMole.setType(.Creature, .Mole)
        excavationMole.trample = true
        excavationMole.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { excavationMole.getController().mill(3) })
        excavationMole.setFlavorText("It makes molehills out of mountains.")
        excavationMole.power = 3
        excavationMole.toughness = 3
        return excavationMole
    }
    static func ExuberantWolfbear() -> Card {
        let exuberantWolfbear = Card(name: "Exuberant Wolfbear", rarity: .Uncommon, set: set, number: 151)
        exuberantWolfbear.setManaCost("3G")
        exuberantWolfbear.setType(.Creature, .Wolf, .Bear)
        exuberantWolfbear.addTriggeredAbility(
            trigger: .ThisAttacks,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isType(.Human) && $0.getController() === exuberantWolfbear.getController() },
                    zones: [.Battlefield]),
                effect: { $0.setBasePowerAndToughnessUntilEndOfTurn(exuberantWolfbear.getPower(), exuberantWolfbear.getToughness()) }))
        exuberantWolfbear.setFlavorText("Sometimes the eludha is a sacred harmony of souls. Sometimes the bonder grabs some fur and holds on tight.")
        exuberantWolfbear.power = 4
        exuberantWolfbear.toughness = 4
        return exuberantWolfbear
    }
    static func Fertilid() -> Card {
        let fertilid = Card(name: "Fertilid", rarity: .Common, set: set, number: 152)
        fertilid.setManaCost("2G")
        fertilid.setType(.Creature, .Elemental)
        fertilid.addReplacementEffect(
            event: .ThisETB,
            effect: { fertilid.addCounters(.PlusOnePlusOne, 2) })
        fertilid.addActivatedAbility(
            string: "{1}{G}, Remove a +1/+1 counter from ~: Target player searches their library for a basic land card, puts it onto the battlefield tapped, then shuffles their library.",
            cost: Cost.Mana("1G").RemoveCounters(.PlusOnePlusOne, 1),
            effect: TargetedEffect.SinglePlayer(
                restriction: TargetingRestriction.TargetPlayer(),
                effect: { target in
                    target.chooseCard(
                        from: target.getLibrary(),
                        restriction: { $0.isBasicLand() },
                        action: { chosen, rest in
                            chosen?.putOntoBattlefield(tapped: true)
                            target.shuffleLibrary()
                    })
            }))
        fertilid.power = 0
        fertilid.toughness = 0
        return fertilid
    }
    // 153 Flycatcher Girrafid
    // 154 Fully Grown
    // 155 Gemrazer
    // 156 Glowstone Recluse
    // 157 Greater Sandwurm
    static func HoneyMammoth() -> Card {
        let honeyMammoth = Card(name: "Honey Mammoth", rarity: .Common, set: set, number: 158)
        honeyMammoth.setManaCost("4GG")
        honeyMammoth.setType(.Creature, .Elephant)
        honeyMammoth.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { honeyMammoth.getController().gainLife(4) })
        honeyMammoth.setFlavorText("todo")
        honeyMammoth.power = 6
        honeyMammoth.toughness = 6
        return honeyMammoth
    }
    // 159 Hornbash Mentor
    // 160 Humble Naturalist
    // 161 Ivy Elemental
    static func KoglaTheTitanApe() -> Card {
        let kogla = Card(name: "Kogla, the Titan Ape", rarity: .Rare, set: set, number: 162)
        kogla.setManaCost("3GGG")
        kogla.setType(.Legendary, .Creature, .Ape)
        kogla.addTriggeredAbility(
            trigger: .ThisETB,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.TargetCreatureYouDontControl(source: kogla, optional: true),
                effect: { kogla.fight($0) } ))
        kogla.addTriggeredAbility(
            trigger: .ThisAttacks,
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isArtifactOrEnchantment() && kogla.getAttackTarget() != nil && $0.getController() === kogla.getAttackTarget()},
                    zones: [.Battlefield]),
                effect: { _ = $0.destroy() }))
        kogla.addActivatedAbility(
            string: "{1}{G}: Return target Human you control to its owner's hand. ~ gains indestructible until end of turn.",
            cost: Cost.Mana("1G"),
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { $0.isType(.Human) && $0.getController() === kogla.getController()},
                    zones: [.Battlefield]),
                effect: { target in
                    target.bounce()
                    kogla.giveKeywordUntilYourNextTurn(.Indestructible)
            }))
        kogla.power = 7
        kogla.toughness = 6
        return kogla
    }
    // 163 Lead the Stampede
    // 164 Migration Path
    // 165 Migratory Greathorn
    // 166 Monstrous Step
    static func MosscoatGoriak() -> Card {
        let mosscoatGoriak = Card(name: "Mosscoat Goriak", rarity: .Common, set: set, number: 167)
        mosscoatGoriak.setManaCost("2G")
        mosscoatGoriak.setType(.Creature, .Beast)
        mosscoatGoriak.vigilance = true
        mosscoatGoriak.setFlavorText("todo")
        mosscoatGoriak.power = 2
        mosscoatGoriak.toughness = 4
        return mosscoatGoriak
    }
    // 168 Mythos of Brokkos
    static func Plummet() -> Card {
        let plummet = Card(name: "Plummet", rarity: .Common, set: set, number: 169)
        plummet.setManaCost("1G")
        plummet.setType(.Instant)
        plummet.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.SingleObject(
                restriction: { return $0.isType(.Creature) && $0.flying },
                zones: [.Battlefield]),
            effect: { target in let _ = target.destroy() }))
        plummet.setFlavorText("todo")
        return plummet
    }
    // 170 Ram Through
    // 171 Sudden Spinnerets
    // 172 Survivors' Bond
    // 173 Thwart the Enemy
    // 174 Titanoth Rex
    // 175 Vivien, Monsters' Advocate
    // 176 Wilt
    // 177 Back for More
    // 178 Boneyard Lurker
    // 179 Brokkos, Apex of Forever
    // 180 Channeled Force
    // 181 Chevill, Bane of Monsters
    // 182 Death's Oasis
    static func DireTactics() -> Card {
        let direTactics = Card(name: "Dire Tactics", rarity: .Uncommon, set: set, number: 183)
        direTactics.setManaCost("WB")
        direTactics.setType(.Instant)
        direTactics.addEffect(TargetedEffect.SingleObject(
            restriction: TargetingRestriction.TargetCreature(),
            effect: { target in
                let targetsToughness = target.getToughness()
                target.exile()
                if !direTactics.getController().controlsA(.Human) {
                    direTactics.getController().loseLife(targetsToughness)
                }
        }))
        direTactics.setFlavorText("\"I will preserve human life at all costs. I will stand alone against behemoths before I let them touch my city. I will hold the wall.\"\n--Oath of the Coppercoats")
        return direTactics
    }
    // 184 Eerie Ultimatum
    // 185 Emergent Ultimatum
    // 186 Frondland Felidar
    // 187 General Kudro of Drannith
    static func GeneralsEnforcer() -> Card {
        let generalsEnforcer = Card(name: "General's Enforcer", rarity: .Uncommon, set: set, number: 188)
        generalsEnforcer.setManaCost("WB")
        generalsEnforcer.setType(.Creature, .Human, .Soldier)
        generalsEnforcer.addStaticAbility(
            requirement: AbilityRequirement{ $0.isType(.Legendary) && $0.isType(.Human) && $0.getController() === generalsEnforcer.getController() },
            effect: { $0.withKeyword(.Indestructible) },
            layer: .AbilityAddingOrRemoving)
        generalsEnforcer.addActivatedAbility(
            string: "{2}{W}{B}: Exile target card from a graveyard. If it was a creature card, create a 1/1 white Human Soldier creature token.",
            cost: Cost.Mana("2WB"),
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.SingleObject(
                    restriction: { _target in true },
                    zones: [.Graveyard]),
                effect: { target in
                    let isCreature = target.isCreature()
                    target.exile()
                    if isCreature {
                        generalsEnforcer.getController().createToken(HumanSoldier())
                    }
            }))
        generalsEnforcer.setFlavorText("More stubborn than Drannith's walls.")
        generalsEnforcer.power = 2
        generalsEnforcer.toughness = 3
        return generalsEnforcer
    }
    // 189 Genesis Ultimatum
    // 190 Illuna, Apex of Wishes
    static func InspiredUltimatum() -> Card {
        let inspiredUltimatum = Card(name: "Inspired Ultimatum", rarity: .Rare, set: set, number: 191)
        inspiredUltimatum.setManaCost("UURRRWW")
        inspiredUltimatum.setType(.Sorcery)
        inspiredUltimatum.addEffect(TargetedEffect(
            restrictions: [
                TargetingRestriction.TargetPlayer(),
                TargetingRestriction.AnyTarget()
            ],
            effect: { targets in
                (targets[0] as! Player).gainLife(5)
                inspiredUltimatum.damage(to: targets[1], 5)
                inspiredUltimatum.getController().drawCards(5)
        }))
        return inspiredUltimatum
    }
    // 192 Kinnan, Bonder Prodigy
    // 193 Labyrinth Raptor
    // 194 Lore Drakkis
    // 195 Narset of the Ancient Way
    // 196 Necropanther
    // 197 Nethroi, Apex of Death
    // 198 Offspring's Revenge
    // 199 Parcelbeast
    // 200 Primal Empathy
    // 201 Quartzwood Crasher
    // 202 Regal Leosaur
    // 203 Rielle, the Everwise
    static func RuinousUltimatum() -> Card {
        let ruinousUltimatum = Card(name: "Ruinous Ultimatum", rarity: .Rare, set: set, number: 204)
        ruinousUltimatum.setManaCost("RRWWWBB")
        ruinousUltimatum.setType(.Sorcery)
        ruinousUltimatum.addEffect({
            ruinousUltimatum.getOpponent().getPermanents().filter({ !$0.isLand() }).forEach({ _ = $0.destroy() })
        })
        ruinousUltimatum.setFlavorText("todo")
        return ruinousUltimatum
    }
    // 205 Savai Thundermane
    static func SkullProphet() -> Card {
        let skullProphet = Card(name: "Skull Prophet", rarity: .Uncommon, set: set, number: 206)
        skullProphet.setManaCost("BG")
        skullProphet.setType(.Creature, .Human, .Druid)
        skullProphet.addActivatedAbility(
            string: "{T}: Add {B}.",
            cost: Cost.Tap(),
            effect: { skullProphet.getController().addMana(color: .Black) })
        skullProphet.addActivatedAbility(
            string: "{T}: Add {G}.",
            cost: Cost.Tap(),
            effect: { skullProphet.getController().addMana(color: .Green) })
        skullProphet.addActivatedAbility(
            string: "{T}: Put the top two cards of your library into your graveyard.",
            cost: Cost.Tap(),
            effect: { skullProphet.getController().mill(2) })
        skullProphet.setFlavorText("TODO")
        skullProphet.power = 3
        skullProphet.toughness = 1
        return skullProphet
    }
    static func SkycatSovereign() -> Card {
        let skycatSovereign = Card(name: "Skycat Sovereign", rarity: .Rare, set: set, number: 207)
        skycatSovereign.setManaCost("WU")
        skycatSovereign.setType(.Creature, .Elemental, .Cat)
        skycatSovereign.flying = true
        skycatSovereign.addStaticAbility(
            requirement: AbilityRequirement.This(skycatSovereign),
            effect: { object in
                let amount = object.getController().getCreatures().filter({ $0 != object && $0.flying }).count
                return object.pumped(amount, amount)
            },
            layer: .PowerToughnessChanging)
        skycatSovereign.addActivatedAbility(
            string: "{2}{W}{U}: Create a 1/1 white Cat Bird creature token with flying.",
            cost: Cost.Mana("2WU"),
            effect: { skycatSovereign.getController().createToken(CatBird()) })
        skycatSovereign.setFlavorText("todo")
        skycatSovereign.power = 1
        skycatSovereign.toughness = 1
        return skycatSovereign
    }
    // 208 Slitherwisp
    // 209 Snapdax, Apex of the Hunt
    // 210 Song of Creation
    static func SpriteDragon() -> Card {
        let spriteDragon = Card(name: "Sprite Dragon", rarity: .Uncommon, set: set, number: 211)
        spriteDragon.setManaCost("UR")
        spriteDragon.setType(.Creature, .Faerie, .Dragon)
        spriteDragon.flying = true
        spriteDragon.addTriggeredAbility(
            trigger: .YouCastNoncreatureSpell,
            effect: { spriteDragon.addCounter(.PlusOnePlusOne) })
        spriteDragon.setFlavorText("Size of a pixie, rage of a hellkite.")
        spriteDragon.power = 1
        spriteDragon.toughness = 1
        return spriteDragon
    }
    // 212 Titans' Nest
    // 213 Trumpeting Gnarr
    // 214 Vadrok, Apex of Thunder
    static func WhirlwindOfThought() -> Card {
        let whirlwindOfThought = Card(name: "Whirlwind of Thought", rarity: .Rare, set: set, number: 215)
        whirlwindOfThought.setManaCost("1URW")
        whirlwindOfThought.setType(.Enchantment)
        whirlwindOfThought.addTriggeredAbility(
            trigger: .YouCastNoncreatureSpell,
            effect: { whirlwindOfThought.getController().drawCard() })
        whirlwindOfThought.setFlavorText("todp")
        return whirlwindOfThought
    }
    // 216 Winota, Joiner of Forces
    // 217 Zenith Flare
    static func AlertHeedbonder() -> Card {
        let alertHeedbonder = Card(name: "Alert Heedbonder", rarity: .Uncommon, set: set, number: 218)
        alertHeedbonder.setManaCost("1{G/W}{G/W}")
        alertHeedbonder.setType(.Creature, .Human, .Scout)
        alertHeedbonder.vigilance = true
        alertHeedbonder.addTriggeredAbility(
            trigger: .YourEndStep,
            effect: {
                let amount = alertHeedbonder.getController().getCreatures().filter({ $0.vigilance }).count
                alertHeedbonder.getController().gainLife(amount)
        })
        alertHeedbonder.setFlavorText("Considering the zealous armies, vile poachers, and titanic monsters, it's always wise to keep one eye on the horizon.")
        alertHeedbonder.power = 2
        alertHeedbonder.toughness = 4
        return alertHeedbonder
    }
    // 219 CunningNightbonder,
    // 220 FiendArtisan,
    // 221 GyrudaDoomOfTheDepths,
    // 222 JeganthaTheWellspring,
    // 223 JubilantSkybonder,
    // 224 KaheeraTheOrphanguard,
    // 225 KerugaTheMacrosage,
    // 226 LurrusOfTheDreamDen,
    // 227 LutriTheSpellchaser,
    // 228 OboshThePreypiercer,
    // 229 ProudWildbonder,
    // 230 SonoroudHowlbonder,
    // 231 UmoriTheCollector,
    // 232 YorionSkyNomad,
    // 233 ZirdaTheDawnbreaker,
    // 234 Crystalline Giant
    // 235 Indatha Crystal
    // 236 Ketria Crystal
    // 237 The Ozolith
    // 238 Raugrin Crystal
    // 239 Savai Crystal
    static func SleeperDart() -> Card {
        let sleeperDart = Card(name: "Sleeper Dart", rarity: .Common, set: set, number: 240)
        sleeperDart.setManaCost("2")
        sleeperDart.setType(.Artifact)
        sleeperDart.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { sleeperDart.getController().drawCard() })
        sleeperDart.addActivatedAbility(
            string: "{T}, Sacrifice ~: Target creature doesn't untap during its controller's next untap step.",
            cost: Cost.Tap().Sacrifice(),
            effect: TargetedEffect.SingleObject(
                restriction: TargetingRestriction.TargetCreature(),
                effect: { $0.untapsDuringNextUntapStep = false }))
        sleeperDart.setFlavorText("\"Sleep now, you big brute.\"\n--Kelsien, the Plague")
        return sleeperDart
    }
    static func SpringjawTrap() -> Card {
        let springjawTrap = Card(name: "Springjaw Trap", rarity: .Common, set: set, number: 241)
        springjawTrap.setManaCost("1")
        springjawTrap.setType(.Artifact)
        springjawTrap.flash = true
        springjawTrap.addActivatedAbility(
            string: "{4}, {T}, Sacrifice ~: ~ deals 3 damage to any target.",
            cost: Cost.Mana("4").Tap().Sacrifice(),
            effect: TargetedEffect(
                restriction: TargetingRestriction.AnyTarget(),
                effect: { springjawTrap.damage(to: $0, 3) }))
        springjawTrap.setFlavorText("todo")
        return springjawTrap
    }
    // 242 Zagoth Crystal
    static func BloodfellCaves() -> Card {
        let bloodfellCaves = Card(name: "Bloodfell Caves", rarity: .Common, set: set, number: 243)
        bloodfellCaves.setManaCost("")
        bloodfellCaves.setType(.Land)
        bloodfellCaves.entersTapped = true
        bloodfellCaves.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { bloodfellCaves.getController().gainLife(1) })
        bloodfellCaves.addActivatedAbility(
            string: "{T}: Add {B}.",
            cost: Cost.Tap(),
            effect: { bloodfellCaves.getController().addMana(color: .Black) },
            manaAbility: true)
        bloodfellCaves.addActivatedAbility(
            string: "{T}: Add {R}.",
            cost: Cost.Tap(),
            effect: { bloodfellCaves.getController().addMana(color: .Red) },
            manaAbility: true)
        return bloodfellCaves
    }
    static func BlossomingSands() -> Card {
        let blossomingSands = Card(name: "Blossoming Sands", rarity: .Common, set: set, number: 244)
        blossomingSands.setManaCost("")
        blossomingSands.setType(.Land)
        blossomingSands.entersTapped = true
        blossomingSands.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { blossomingSands.getController().gainLife(1) })
        blossomingSands.addActivatedAbility(
            string: "{T}: Add {G}.",
            cost: Cost.Tap(),
            effect: { blossomingSands.getController().addMana(color: .Green) },
            manaAbility: true)
        blossomingSands.addActivatedAbility(
            string: "{T}: Add {W}.",
            cost: Cost.Tap(),
            effect: { blossomingSands.getController().addMana(color: .White) },
            manaAbility: true)
        return blossomingSands
    }
    // 245 Bonder's Enclave
    static func DismalBackwater() -> Card {
        let dismalBackwater = Card(name: "Dismal Backwater", rarity: .Common, set: set, number: 246)
        dismalBackwater.setManaCost("")
        dismalBackwater.setType(.Land)
        dismalBackwater.entersTapped = true
        dismalBackwater.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { dismalBackwater.getController().gainLife(1) })
        dismalBackwater.addActivatedAbility(
            string: "{T}: Add {U}.",
            cost: Cost.Tap(),
            effect: { dismalBackwater.getController().addMana(color: .Blue) },
            manaAbility: true)
        dismalBackwater.addActivatedAbility(
            string: "{T}: Add {B}.",
            cost: Cost.Tap(),
            effect: { dismalBackwater.getController().addMana(color: .Black) },
            manaAbility: true)
        return dismalBackwater
    }
    static func EvolvingWilds() -> Card {
        let evolvingWilds = Card(name: "Evolving Wilds", rarity: .Common, set: set, number: 247)
        evolvingWilds.setManaCost("")
        evolvingWilds.setType(.Land)
        evolvingWilds.addActivatedAbility(
            string: "{T}, Sacrifice ~: Search your library for a basic land card, put it onto the battlefield tapped, then shuffle your library.",
            cost: Cost.Tap().Sacrifice(),
            effect: { evolvingWilds.getController().chooseCard(
                from: evolvingWilds.getController().getLibrary(),
                restriction: { $0.isBasicLand() },
                action: { chosen, rest in
                    chosen?.putOntoBattlefield(tapped: true)
                    evolvingWilds.getController().shuffleLibrary()
            })
        })
        evolvingWilds.setFlavorText("\"It's not hard to grasp that this land does not belong to us.\"\n--Gavi, nest warden")
        return evolvingWilds
    }
    // 248 Indatha Triome
    static func JungleHollow() -> Card {
        let jungleHollow = Card(name: "Jungle Hollow", rarity: .Common, set: set, number: 249)
        jungleHollow.setManaCost("")
        jungleHollow.setType(.Land)
        jungleHollow.entersTapped = true
        jungleHollow.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { jungleHollow.getController().gainLife(1) })
        jungleHollow.addActivatedAbility(
            string: "{T}: Add {B}.",
            cost: Cost.Tap(),
            effect: { jungleHollow.getController().addMana(color: .Black) },
            manaAbility: true)
        jungleHollow.addActivatedAbility(
            string: "{T}: Add {G}.",
            cost: Cost.Tap(),
            effect: { jungleHollow.getController().addMana(color: .Green) },
            manaAbility: true)
        return jungleHollow
    }
    // 250 Ketria Triome
    // 251 Raugrin Triome
    static func RuggedHighlands() -> Card {
        let ruggedHighlands = Card(name: "Rugged Highlands", rarity: .Common, set: set, number: 252)
        ruggedHighlands.setManaCost("")
        ruggedHighlands.setType(.Land)
        ruggedHighlands.entersTapped = true
        ruggedHighlands.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { ruggedHighlands.getController().gainLife(1) })
        ruggedHighlands.addActivatedAbility(
            string: "{T}: Add {R}.",
            cost: Cost.Tap(),
            effect: { ruggedHighlands.getController().addMana(color: .Red) },
            manaAbility: true)
        ruggedHighlands.addActivatedAbility(
            string: "{T}: Add {G}.",
            cost: Cost.Tap(),
            effect: { ruggedHighlands.getController().addMana(color: .Green) },
            manaAbility: true)
        return ruggedHighlands
    }
    // 253 Savai Triome
    static func ScouredBarrens() -> Card {
        let scouredBarrens = Card(name: "Scoured Barrens", rarity: .Common, set: set, number: 254)
        scouredBarrens.setManaCost("")
        scouredBarrens.setType(.Land)
        scouredBarrens.entersTapped = true
        scouredBarrens.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { scouredBarrens.getController().gainLife(1) })
        scouredBarrens.addActivatedAbility(
            string: "{T}: Add {W}.",
            cost: Cost.Tap(),
            effect: { scouredBarrens.getController().addMana(color: .White) },
            manaAbility: true)
        scouredBarrens.addActivatedAbility(
            string: "{T}: Add {B}.",
            cost: Cost.Tap(),
            effect: { scouredBarrens.getController().addMana(color: .Black) },
            manaAbility: true)
        return scouredBarrens
    }
    static func SwiftwaterCliffs() -> Card {
        let swiftwaterCliffs = Card(name: "Swiftwater Cliffs", rarity: .Common, set: set, number: 255)
        swiftwaterCliffs.setManaCost("")
        swiftwaterCliffs.setType(.Land)
        swiftwaterCliffs.entersTapped = true
        swiftwaterCliffs.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { swiftwaterCliffs.getController().gainLife(1) })
        swiftwaterCliffs.addActivatedAbility(
            string: "{T}: Add {U}.",
            cost: Cost.Tap(),
            effect: { swiftwaterCliffs.getController().addMana(color: .Blue) },
            manaAbility: true)
        swiftwaterCliffs.addActivatedAbility(
            string: "{T}: Add {R}.",
            cost: Cost.Tap(),
            effect: { swiftwaterCliffs.getController().addMana(color: .Red) },
            manaAbility: true)
        return swiftwaterCliffs
    }
    static func ThornwoodFalls() -> Card {
        let thornwoodFalls = Card(name: "Thornwood Falls", rarity: .Common, set: set, number: 256)
        thornwoodFalls.setManaCost("")
        thornwoodFalls.setType(.Land)
        thornwoodFalls.entersTapped = true
        thornwoodFalls.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { thornwoodFalls.getController().gainLife(1) })
        thornwoodFalls.addActivatedAbility(
            string: "{T}: Add {G}.",
            cost: Cost.Tap(),
            effect: { thornwoodFalls.getController().addMana(color: .Green) },
            manaAbility: true)
        thornwoodFalls.addActivatedAbility(
            string: "{T}: Add {U}.",
            cost: Cost.Tap(),
            effect: { thornwoodFalls.getController().addMana(color: .Blue) },
            manaAbility: true)
        return thornwoodFalls
    }
    static func TranquilCove() -> Card {
        let tranquilCove = Card(name: "Tranquil Cove", rarity: .Common, set: set, number: 257)
        tranquilCove.setManaCost("")
        tranquilCove.setType(.Land)
        tranquilCove.entersTapped = true
        tranquilCove.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { tranquilCove.getController().gainLife(1) })
        tranquilCove.addActivatedAbility(
            string: "{T}: Add {W}.",
            cost: Cost.Tap(),
            effect: { tranquilCove.getController().addMana(color: .White) },
            manaAbility: true)
        tranquilCove.addActivatedAbility(
            string: "{T}: Add {U}.",
            cost: Cost.Tap(),
            effect: { tranquilCove.getController().addMana(color: .Blue) },
            manaAbility: true)
        return tranquilCove
    }
    static func WindScarredCrag() -> Card {
        let windScarredCrag = Card(name: "Wind-Scarred Crag", rarity: .Common, set: set, number: 258)
        windScarredCrag.setManaCost("")
        windScarredCrag.setType(.Land)
        windScarredCrag.entersTapped = true
        windScarredCrag.addTriggeredAbility(
            trigger: .ThisETB,
            effect: { windScarredCrag.getController().gainLife(1) })
        windScarredCrag.addActivatedAbility(
            string: "{T}: Add {R}.",
            cost: Cost.Tap(),
            effect: { windScarredCrag.getController().addMana(color: .Red) },
            manaAbility: true)
        windScarredCrag.addActivatedAbility(
            string: "{T}: Add {W}.",
            cost: Cost.Tap(),
            effect: { windScarredCrag.getController().addMana(color: .White) },
            manaAbility: true)
        return windScarredCrag
    }
    // 259 Zagoth Triome
    
    static func Cat() -> Token {
        let cat = Token(name: "Cat Bird", set: set, number: 1)
        cat.colors = [.White]
        cat.setType(.Creature, .Cat)
        cat.lifelink = true
        cat.power = 1
        cat.toughness = 1
        return cat
    }
    static func CatBird() -> Token {
        let catBird = Token(name: "Cat Bird", set: set, number: 2)
        catBird.colors = [.White]
        catBird.setType(.Creature, .Cat, .Bird)
        catBird.flying = true
        catBird.power = 1
        catBird.toughness = 1
        return catBird
    }
    static func HumanSoldier() -> Token {
        let r = Int.random(in: 3 ... 5)
        let humanSoldier = Token(name: "Human Soldier", set: set, number: r)
        humanSoldier.colors = [.White]
        humanSoldier.setType(.Creature, .Human, .Soldier)
        humanSoldier.power = 1
        humanSoldier.toughness = 1
        return humanSoldier
    }
    // T6
    // T7
    static func Dinosaur() -> Token {
        let dinosaur = Token(name: "Dinosaur", set: set, number: 8)
        dinosaur.colors = [.Red]
        dinosaur.setType(.Creature, .Dinosaur)
        dinosaur.haste = true
        dinosaur.power = 1
        dinosaur.toughness = 1
        return dinosaur
    }

}
